# 2026-04-23 — 자율모드 세션 (OMC/OMX gap 흡수)

## 큐 순서
B1 → B2 → B4 → B3 → B5

## 상태
자율모드 진입: 2026-04-23, `CLAUDE.md §21` 준수.
제한: feature branch 생략 (main 직접 커밋, 기존 프로젝트 패턴), main push 금지는 기존 사용자 명시 허용 맥락에서 유지.

---

## B1 · 실행 모드 프리셋 4종 (bt-autopilot / bt-ralph / bt-ultrawork / bt-deep-interview) — 완료
- 변경: `server/workflows.py::BUILTIN_TEMPLATES` 4 항목 추가 (5·5·8·5 노드)
- bt-ralph 는 `repeat.enabled=True, maxIterations=5` + feedback node 설정 → verify fail 시 자동 loop
- 검증: 4 템플릿 모두 `/api/workflows/templates/{id}` 정상 조회, 총 템플릿 수 5→9

## B2 · Prompt Library 키워드 트리거 — 완료
- 변경:
  - `server/prompt_library.py::api_prompt_library_save` 에 `keywords` 필드 추가
  - `server/prompt_library.py::find_keyword_triggers()` 신규 — 입력 텍스트에서 매칭 검사
  - `server/workflows.py` session 노드 실행 시 systemPrompt 앞에 매칭 프롬프트 자동 prepend
  - `dist/index.html::plSave` + 에디터 UI 에 `keywordsRaw` 입력 필드
- 검증: unit 테스트로 save → trigger hit / miss / cleanup 확인

## B4 · session 노드 modelHint 자동 라우팅 — 완료
- 변경:
  - `_sanitize_node` 에 `modelHint` 필드 (`auto|fast|deep|""`)
  - session 실행 시 assignee 비어있으면 prompt 길이 + 키워드 기반 자동 선택
    - fast → haiku, deep → opus, auto → 휴리스틱 (길이/키워드)
  - 실행 결과에 `chosenModel` 필드 노출
- 검증: sanitize 단위 테스트 (auto/invalid/deep) 통과

## B3 · 워크플로우 완료 Slack/Discord 알림 — 완료
- 신규: `server/notify.py`
  - 호스트 화이트리스트 (`hooks.slack.com`, `discord.com`, `discordapp.com`)
  - `notify_workflow_completion()` — run 종료 시 이모지+상태+비용+시간 전송
  - `api_notify_test()` — UI 테스트 버튼 엔드포인트
- `_sanitize_workflow` 에 `notify: {slack, discord}` 필드 추가 (https 접두 + 길이 제한)
- `_run_workflow_background` 성공/실패 분기에서 `_notify_run_completion()` 호출 (조용 실패)
- 에디터 인스펙터에 두 URL 입력 + 테스트 버튼 추가
- 검증: curl 로 evil.com 거부, bad target 거부 확인

## B5 · Session Replay Lab — 완료
- 신규 모듈: `server/session_replay.py`
  - `api_session_replay_list` — `~/.claude/projects/**/*.jsonl` 최근 50건
  - `api_session_replay_load` — JSONL 파싱 (최대 2000 events)
  - ISO timestamp 파싱, message.role/content 요약, tool_use/tool_result 마커
- 신규 탭 `sessionReplay` (work 그룹) + nav_catalog + NAV 엔트리 + i18n (en/zh)
- VIEWS.sessionReplay: 좌측 세션 목록 + 우측 타임라인 + 토큰 누적 스파크라인 (600×40 SVG)
- 54/54 탭 smoke 통과

---

## 세션 요약 (23:00 ~ )
- 처리 완료: 5건 (B1~B5, 모두 low 위험)
- 블록됨: 0건
- 실패: 0건
- 생성 커밋: (대기 — 일괄 커밋 예정)
- 남은 큐: 0 (today-queue.md 전체 소진)
- 종료 사유: 큐 소진
