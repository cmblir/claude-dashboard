# 2026-05-18 — lazyclaw ↔ lazyclaude 레포 분리

## 무엇을 / 왜

v3.99.26에서 lazyclaude 대시보드에 16탭으로 통합됐던 lazyclaw CLI를 별도
레포로 완전 분리. 두 산출물의 강결합 해소 + lazyclaw 독립 배포 라인 확보.

- 신규 레포: `/Users/yoo/project/lazyclaw` → GitHub `cmblir/lazyclaw` (public).
  fresh `git init`, 2커밋(import + release 3.99.29). publish 워크플로 root 기준 적응.
- lazyclaude(`feat/separate-lazyclaw`): proxy/routes/16탭/e2e/i18n/Makefile/README
  에서 lazyclaw 전면 제거. v3.99.29.

## 예상과 달랐던 점

1. `/Users/yoo/project/lazyclaude` == `LazyClaude` (macOS 대소문자 비구분 동일 디렉터리).
2. `lazyclaw@3.99.28` 이미 npm 게시 → 신규 레포 첫 push가 publish 가드에 막힘 →
   사용자 결정으로 3.99.29 범프. **`NPM_TOKEN` secret 미설정 → 첫 CI publish job 실패
   예정**(레포 생성·push는 성공). 사용자가 secret 등록해야 자동 퍼블리시 작동.
3. `CLAUDE.md`상 `dist/index.html ~18000줄`이라 했으나 실제 1396줄 — UI 로직은
   `dist/app.js`(28401줄)에 존재. lazyclaw 블록은 파일 말미 785줄.
4. 마스코트(🦞)는 lazyclaw 기능이 아니라 **일반 대시보드 chrome**(workflow/modal/chat
   상태 피드 40+ 콜사이트). 제거 시 무관 기능 파손 → 보존, 주석만 de-brand.
5. `🦞 Ralph Loop`도 lobster 이모지를 쓰는 별개 기능 — 미제거.
6. `translations_manual_42`는 권한/이메일 + lazyclaw 키 혼재. `_44`의 2키
   ("확인 중…","연결 확인")는 **일반 대시보드 chat에서 사용 중** → `_44` 보존,
   `_43`(전부 고아)·`_42`의 lazyclaw 키 2개만 제거. 추측 아닌 실측으로 판정.
7. `build_locales.py`는 기존 locale json을 머지(prune 안 함) → stale lazyclaw 키
   2개를 json에서 직접 제거해야 했음.
8. Obsidian MCP 미연결 세션 → `Projects/lazyclaude/`(ADR/log/qa)에 기록.

## 검증

- 백엔드: `from server.routes import ...` OK, `/api/lc/` 라우트 0개.
- i18n: `make i18n-verify` ✓ 모든 검증 통과. dist/locales lazyclaw 0 (4747→4745).
- UI: Playwright MCP 3뷰포트(1280/768/375) 전부 PASS — 콘솔 에러 0, lazyclaw
  nav/텍스트 0, stale `lc*`→overview 폴백, core 탭 무회귀, 가로 오버플로 없음.
  → `qa/2026-05-18-lazyclaw-removal.md`.
- 신규 레포: `node cli.mjs --version` → 3.99.29, 전 모듈 `node --check` OK,
  executor 동적 import OK.

## 다음 참고 포인트

- **사용자 액션 필요**: GitHub `cmblir/lazyclaw` Settings → Secrets → `NPM_TOKEN`
  (granular, lazyclaw write + Bypass 2FA) 등록해야 publish 워크플로 성공.
- lazyclaude `feat/separate-lazyclaw` 브랜치 — PR/병합은 사용자 판단(여기선 main
  푸시 안 함).
- 잔존 lazyclaw 텍스트(의도): README 출처 안내 1줄 ×3, app.js 레거시
  localStorage 마이그레이션 + npm 안내 주석, CHANGELOG/Projects(이력).
- 비배포 스크래치(.ralph-progress.md/ralph-prompt.md/BLOCKERS.md)의 lazyclaw
  언급은 과거 작업 이력 — 이력 위조 금지 원칙상 보존.
