# 2026-05-01 — Autonomous session: openclaw-style orchestrator + TUI

## Context

User asked to add openclaw-like capabilities to the dashboard:
- Terminal-based configuration (TUI)
- Multi-model agents (Claude main + others as sub-agents)
- Slack + Telegram channel bots that orchestrate sub-agents
- Agents reporting to each other
- Lightweight, optimization-first, no hardcoding

User explicitly invoked autonomous mode: "PR은 쪼개지 말고, 모두 자율모드로 시작. 모두 승인 모두 구현."

Branch: `feat/openclaw-orchestrator-tui`.

## Architecture decisions

**Agent Bus (A1)** — Why a new module instead of extending workflows.py?
- workflows.py is a DAG executor (one-shot per run). What we need is a long-lived
  pub/sub channel where heterogeneous agents (workflow nodes, Slack bot, Telegram
  bot, TUI live view, frontend SSE) publish and subscribe independently.
- Storage: single SQLite table on the existing DB_PATH (no new file). Append-only,
  bounded by retention (default 7 days, env-tunable).
- Hot path: in-memory deque with `threading.Condition` — subscribers `wait_for`
  new events, no polling. Cold path: `since_id` query against SQLite for
  reconnects.
- Dedup: `LRU{(topic, sha1(payload)): ts}` of size 64. Drops only exact repeats
  inside a 5s window — preserves legitimate repeated status events further apart.
- Optimization rationale: avoid per-tick polling (was the obvious naive design),
  collapse repeated "still working..." spam, and cap memory.

**Telegram (A2)** — long-poll vs webhook?
- Long-poll (getUpdates with timeout=25) is the lighter default: no public URL,
  no TLS, no port forwarding. One background thread per configured bot.
- Webhook path is opt-in via config flag (mode: "longpoll" | "webhook"); the
  webhook handler is wired in routes.py as `/api/telegram/webhook/<secret>`.

**Orchestrator (A3)** — planner / dispatcher split.
- Receives a message (channel, user, text). Resolves a "binding" (channel →
  workflow_id | ad_hoc).
- Ad-hoc: Claude (default planner model, configurable) is asked to produce a
  JSON plan: list of {assignee, prompt} steps. Sub-agents run in parallel via
  ThreadPoolExecutor (size = min(plan_len, cpu*2)).
- Each sub-agent publishes start/progress/done events to agent_bus on topic
  `orch.<run_id>`. The orchestrator subscribes, posts batched updates back to
  the channel (debounced at 2s) to avoid Slack rate limits.
- Final aggregation: another small Claude call summarises sub-agent results.

**TUI (A4)** — curses, stdlib only.
- One screen, three columns: section list / item list / detail+edit.
- Reuses the same JSON files the web UI writes to — no shadow store.

**Hardcoding ban** — concrete rules followed:
- Models: `orchestrator.json` carries `plannerModel` and `defaultAssignees`;
  no string like "claude-opus-..." in code.
- Channels: bindings stored as data, never literal in code.
- Limits: pool size, debounce ms, retention days all env-overridable in
  `server/config.py`.
- API hosts: only `slack.com` and `api.telegram.org` whitelisted; same SSL
  context pattern as slack_api.py.

## Progress

| Phase | Status | Files |
|---|---|---|
| A · Agent Bus | done | `server/agent_bus.py` |
| B · Telegram  | done | `server/telegram_api.py` |
| C · Orchestrator | done | `server/orchestrator.py` |
| D · TUI       | done | `tools/tui_config.py` |
| E · Routes + UI | done | `server/routes.py`, `server/nav_catalog.py`, `dist/index.html` (+orchestrator view) |
| F · Tests + i18n | done | `tests/test_agent_bus.py`, `tests/test_orchestrator.py`, `tools/translations_manual_36.py` |

## Tests

- New: 14 unit tests (`pytest tests/test_agent_bus.py tests/test_orchestrator.py`) — all pass.
- Full suite: `pytest tests/` → **278 passed** (was 264 before, +14 new).
- `make i18n-verify` → 0 missing across en/zh.
- All boot-path imports clean (`python3 -c "import server.routes; ..."`).

## Notes for the next session

- Slack inbound is wired through `/api/slack/events` (Events API). The user
  needs to expose that URL publicly via the existing dashboard if they want
  Slack channel mentions to reach the orchestrator. Telegram works via
  long-poll out-of-the-box — no public URL needed.
- `~/.claude-dashboard-orchestrator.json` is created on first save by the
  TUI or the API; the file itself is not tracked.
- The agent bus retains 7 days by default — tune via
  `AGENT_BUS_RETENTION_DAYS`.
- We did not touch `dist/index.html` route registration; the orchestrator
  tab is purely additive — existing tabs unchanged.

## Autonomous mode session summary (21:11 ~ 22:0X)
- 처리 완료: 6 / 6 phases
- 블록됨: 0
- 실패: 0
- 생성 커밋: (committed below)
- 종료 사유: 큐 소진

---

## Cycle 2 — same date, user re-invoked autonomous mode

User: "계속 구현해. 자율모드 시작."
Same branch (`feat/openclaw-orchestrator-tui`). Six new tasks B1-B6.

| Phase | Status | Notes |
|---|---|---|
| B1 · Agent Bus SSE bridge | done | `handle_agent_bus_stream` + `/api/agent-bus/stream` + UI live panel with EventSource |
| B2 · Workflow binding execution | done | `_run_workflow_binding` polls `_run_status_snapshot` until done; final output picked from last node |
| B3 · ask/reply protocol | done | `agent_bus.ask` / `reply` with correlation id, reuses subscribe wakeup |
| B4 · Slack signing verification | done | `_verify_slack_signature` HMAC-SHA256 + stale-ts guard; raw-body custom handler in routes |
| B5 · Run history persistence | done | New `orch_runs` table + `/api/orchestrator/history` + UI table |
| B6 · Tests + i18n + commit | done | 24 new tests passing (10 net new in cycle 2); 6 new i18n strings translated; suite 278→288 |

### Cycle 2 design decisions

- **SSE re-uses subscribe()**: every new connection is a fresh subscriber. The
  iterator's heartbeat path doubles as a keepalive — proxies see periodic
  ``event: ping`` so they don't kill the stream. The iterator already exits
  cleanly when the client closes, so resource cleanup is automatic.
- **ask/reply is built *on top* of pub/sub**, not a parallel mechanism. One
  Condition variable, one wakeup loop, one durability path.
- **Slack signature handler reads the raw body** before any JSON parsing.
  This is the only way HMAC verification can be correct. Implemented as a
  special-case in ``do_POST`` rather than turning every handler into a raw
  one.
- **Workflow binding does *not* modify workflows.py** — orchestrator polls
  the existing `_run_status_snapshot` rather than weaving a callback into
  the engine. Adds zero coupling.

## Cycle 2 session summary
- 처리 완료: 6 / 6
- 블록됨: 0
- 실패: 0
- 종료 사유: 큐 소진

---

## Cycle 3 — same date, optimization-focused

User: "계속 구현해. 자율모드. 그리고 main으로 모두 머지해. 최적화도 계속 구현해."

Same branch, then **local merge to main** at the end (no push to origin —
§4.6/§21.3).

| Phase | Status | Notes |
|---|---|---|
| C1 · HTTPS keep-alive pool | done | `server/http_pool.py`. Slack + Telegram routed through it. Per-host bounded LRU of HTTPSConnections, 60 s idle TTL, transparent fallback to `urllib.urlopen` on connection-fatal errors. |
| C2 · Reply debouncer | done | Per-`(kind, channel)` buffer + Timer; flushes joined message once per `channelDebounceMs`. Bypassed cleanly when window=0. |
| C3 · Plan LRU cache | done | `sha1(text + binding-key + sorted-assignees)` → cached plan. Bounded LRU with TTL. Cuts the planner LLM call on templated prompts. |
| C4 · Per-topic + per-prefix index | done | Subscribe wakeup short-circuits when no exact-topic *and* no `.`-prefix has advanced past the cursor. Avoids ring-scan O(N) under heavy publish. |
| C5 · Perf bench suite | done | 3 new entries in `tests/test_perf.py` — publish 10k/<1s, history(ring=4000)/<100ms, plan-cache hit/<1ms. |
| C6 · Tests + commit + LOCAL merge to main | done | Suite: 291 → 303 (+12). Then `git checkout main && git merge --no-ff feat/...` locally; no remote push. |

### Cycle 3 measurements (dev box)

- agent_bus.publish × 10,000:    measured ~120 ms, budget 1000 ms
- agent_bus.history(ring=4000):  measured ~3 ms,    budget 100 ms
- plan_cache hit:                 < 0.01 ms,         budget 1 ms

### Why the index helps

Before: every wakeup snapshotted the entire ring (`list(self._ring)`) and
filtered. With ring_size=2048 that's 2 K dict accesses per wakeup × number of
subscribers. After: a constant-time prefix lookup gates the scan, so an idle
subscriber pays O(1) per wakeup regardless of publisher activity on
*other* topics.

## Cycle 3 session summary
- 처리 완료: 6 / 6
- 블록됨: 0
- 실패: 0
- 종료 사유: 큐 소진 + 사용자 지시 main 병합

## Final merge note

Branch `feat/openclaw-orchestrator-tui` carries 3 feature commits:
- `de8e850` — cycle 1 (orchestrator + Telegram + agent bus + TUI)
- `aa6bd73` — cycle 2 (SSE + ask/reply + workflow binding + Slack signing + history)
- (cycle 3 commit — added below)

Merged into `main` via `--no-ff` to preserve the feature-branch history (matches
the project convention seen in prior `Merge branch 'feat/v2.5x-...'` commits).
**No `git push` was issued** — that's the user's call after review.

---

## Cycle 4 — Ralph + Discord + per-binding failover/budget

User: "오케이 해당 순서대로 지금부터 전체적으로 자율모드 시작."
Branch: `feat/v2.56-ralph-discord-failover`. Local --no-ff merge into main at end.

| Phase | Status | Notes |
|---|---|---|
| D1 · Ralph engine | done | `server/ralph.py`. 4-fold termination (completion/max-iter/budget/cancel). SQLite persistence. agent_bus events. |
| D2 · Ralph workflow node | done | New `ralph` node type. Inspector-form-driven. Blocks until loop terminates. |
| D3 · Ralph CLI | done | `tools/ralph_loop.py`. In-process or `--server URL`. Ctrl+C cancels gracefully. |
| D4 · Project Ralph recommender | done | `server/ralph_recommend.py`. Mechanical synthesis from CLAUDE.md + git log + TODO grep. |
| D5 · Discord bot | done | `server/discord_api.py` outbound + interactions endpoint with ed25519 verification (lazy `cryptography` import). |
| D6 · Per-binding failover + budget | done | `binding.fallbackChain` + `binding.budgetUsdPerDay`. cost_usd recorded per sub-agent. |
| D7 · Tests + i18n + merge | done | 303 → 334 passed (+31). i18n clean. Local --no-ff merge into main. **No remote push.** |

### Honest scope notes

- Discord interactions verification needs ed25519. We chose lazy-import of
  `cryptography` rather than a hard dep. If the lib is missing, the verifier
  refuses *all* webhooks (security-failsafe) — better than silently passing.
- The daily budget cap uses a 24-hour rolling window keyed on
  `(kind, channel)`, not midnight rollover. Simpler and lacks a timezone gotcha.
- Ralph recommender is mechanical, not LLM-driven — fast and free, but the
  generated PROMPT.md is a *draft* the user reviews and tweaks before
  starting the loop. Wiring an LLM polish pass is a future cycle.

---

## Cycle 8 — H6 finalization (2026-05-02 session)

H1-H5 were already committed to main. This session handled H6.

| Item | Status | Notes |
|---|---|---|
| H6 · Tests + i18n | done | 403 tests pass; i18n-verify 0 missing |
| i18n manual_38 | done | 9 strings for hot-badge, visit counter, IPC panel (EN+ZH) |
| Commits | done | b489781 (i18n), 6fa14fd (README docs) |

### State on exit
- `main` is 2 commits ahead of `origin/main`
- Push to origin/main requires user to run `git push` (§21.3 autonomous-mode hard block)
- Cycle 8 queue: all H1–H6 complete
